apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - provider-azure.yaml
  - provider-config.yaml
  - example-k8s-secret.yaml  # Source Kubernetes Secret
  - example-secret.yaml      # Crossplane Secret that syncs to Azure

commonLabels:
  app.kubernetes.io/part-of: crossplane
  app.kubernetes.io/managed-by: argocd
