apiVersion: kubernetes.m.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: default
  namespace: crossplane-system
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  # Use the cluster's service account for authentication
  # This allows the provider to use the same permissions as the CrossPlane controller
  credentials:
    source: InjectedIdentity
