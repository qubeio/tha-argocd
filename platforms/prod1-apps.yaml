apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod1-apps
  namespace: argocd
spec:
  project: fleet-manager
  source:
    repoURL: https://github.com/qubeio/tha-argocd.git
    targetRevision: HEAD
    path: applications/prod1
    directory:
      # Include all YAML files in applications/prod1/ directory
      include: "*.yaml"
  destination:
    # This will target the vcluster once it's registered
    # The vcluster will be available at: 
    # https://prod1.vcluster-prod1.svc
    name: prod1
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  # Wait for vcluster to be ready before syncing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 10m

